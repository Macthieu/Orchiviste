# S3.5 Bootstrap (consolidation legere)

Date: 2026-04-18
Perimetre initial: Orchiviste uniquement (documentaire + operationnel leger)

## Objectif de S3.5
Consolider l'exploitation post-S3.4 sans ouvrir de nouveau chantier technique: clarifier la pratique operateur, stabiliser les conventions de pilotage, et preparer un passage propre vers une future phase d'implementation plus large si necessaire.

## Plus petit perimetre utile
- Verifier la coherence documentaire entre runbook, playbook incident, profils operatoires et signaux UI `Decision Apply`.
- Figer une checklist operateur unique "avant apply" pour eviter les interpretations divergentes.
- Definir un point de controle simple pour decider si une evolution UI/backend est vraiment necessaire ensuite.

## Hors perimetre explicite
- Aucun changement backend.
- Aucun changement UI.
- Aucun refactor.
- Aucun nouveau smoke, workflow CI, ou contrat.
- Aucun changement sur les autres depots Muni.
- Le stash reste hors perimetre et inchange:
  - `stash@{0}: wip(ui): cockpit.leaf en attente validation visuelle`

## Dependances (S3.1 -> S3.4)
- `S3.1`: runbook + profils operatoires + playbook incident.
- `S3.2`: referentiel diagnostics + matrice UI + contrat UI minimal.
- `S3.3`: readiness UI + patch minimal Pilotage/Historique.
- `S3.4`: decision UI `Go/No-Go` dans Lancer/Historique + notes operations + cloture.

## Risques immediats
- Derive de scope vers des modifications techniques non necessaires.
- Divergence entre documentation et usage operateur reel.
- Multiplication de documents sans regle de priorite claire.
- Reouverture implicite de sujets deja clos en S3.3/S3.4.

## Ordre minimal recommande
1. Ouvrir `S3.5-T1` en mode strictement documentaire (checklist operateur unique).
2. Verifier l'alignement de toutes les references S3.1/S3.2/S3.4 depuis un point d'entree unique.
3. Clore S3.5 rapidement si aucun blocage operationnel nouveau n'est identifie.

## Decision proposee
Ouvrir S3.5 comme phase courte de consolidation legere, sans implementation technique.

## Pret a ouvrir S3.5-T1
Oui. Lot minimal propose:
- creer `docs/S3.5-APPLY-READINESS-CHECKLIST.md` avec une checklist operateur unique, compacte, basee sur les diagnostics visibles et les garde-fous deja existants.
